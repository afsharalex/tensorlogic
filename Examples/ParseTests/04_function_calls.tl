// Example 4: Function Calls (Nonlinearities)
// Various activation functions

// Sigmoid activation
Y1[i] = sigmoid(X[i])

// ReLU activation
Y2[i] = relu(X[i])

// Tanh activation
Y3[i] = tanh(X[i])

// Softmax (normalization)
Y4[i.] = softmax(X[i])

// Step function (Heaviside)
Y5[i] = step(X[i])
