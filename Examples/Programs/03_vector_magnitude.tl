// ============================================
// Vector Magnitude using Inner Product
// ============================================
// The magnitude of a vector is sqrt(dot(v, v))

// Define a vector
V = [3.0, 4.0]

// Compute squared magnitude (inner product with itself)
squared_magnitude = V[i] V[i]

// Compute actual magnitude using square root
magnitude = sqrt(squared_magnitude)

squared_magnitude?  // Expected: 9.0 + 16.0 = 25.0
magnitude?          // Expected: 5.0